{
  "name": "agros",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd apps/backend && npm ci && cd ../frontend && npm ci",
    "build:frontend": "cd apps/frontend && npm run build",
    "build:backend": "cd apps/backend && npm run build",
    "build": "npm run build:frontend && npm run build:backend",
    "start": "cd apps/backend && npm start",
    "dev": "echo 'Run backend and frontend separately — see apps/backend and apps/frontend'"
  }
}

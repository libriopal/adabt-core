# ── Server ──────────────────────────────────────────────
PORT=3001
NODE_ENV=production

# ── Workers (require Redis) ─────────────────────────────
# Set to "false" to disable BullMQ background workers
ENABLE_WORKERS=false

# Only needed if ENABLE_WORKERS is not "false"
# REDIS_HOST=your-redis-host
# REDIS_PORT=6379
